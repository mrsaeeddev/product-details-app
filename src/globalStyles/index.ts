import { StyleSheet } from "react-native"

export const globalStyles = StyleSheet.create({
    container: {
        padding: 20
    },
    textCenter: {
        textAlign: "center",
    },
    alignItemsCenter: {
        alignItems: "center",
        justifyContent: "center"
    }
});

